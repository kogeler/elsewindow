[pytest]
addopts = -n auto --dist=worksteal --cov --cov-report=term --cov-report=xml
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    host: requires host Podman or another operator-controlled host facility
